import { CustomisationRequest } from './customisation-request';

describe('CustomisationRequest', () => {
  it('should create an instance', () => {
    expect(new CustomisationRequest()).toBeTruthy();
  });
});
